# 二手车拍卖平台 - 外网部署指南

## 快速部署步骤

### 1. 构建生产版本

在项目根目录执行：

```bash
npm run build
```

构建完成后，会在项目根目录生成 `dist` 文件夹，包含所有静态文件。

### 2. 部署选项

#### 选项 A: 使用 Vercel（推荐，免费且简单）

1. 访问 [vercel.com](https://vercel.com)
2. 使用 GitHub/GitLab 账号登录
3. 点击 "Add New Project"
4. 导入您的项目仓库
5. Vercel 会自动检测 Vite 项目并配置
6. 点击 "Deploy"
7. 几分钟后获得外网访问链接，例如：`https://your-project.vercel.app`

**优点：**
- 完全免费
- 自动 HTTPS
- 全球 CDN 加速
- 自动部署（推送代码即自动更新）

#### 选项 B: 使用 Netlify（免费）

1. 访问 [netlify.com](https://netlify.com)
2. 注册/登录账号
3. 拖拽 `dist` 文件夹到 Netlify 页面
4. 或者连接 Git 仓库自动部署
5. 获得外网访问链接，例如：`https://your-project.netlify.app`

**优点：**
- 完全免费
- 拖拽部署，超级简单
- 自动 HTTPS
- 支持自定义域名

#### 选项 C: 使用 GitHub Pages（免费）

1. 在项目根目录创建 `.github/workflows/deploy.yml`
2. 推送代码到 GitHub
3. 在仓库设置中启用 GitHub Pages
4. 访问链接：`https://your-username.github.io/your-repo-name`

**配置文件内容：**

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          
      - name: Install dependencies
        run: npm install
        
      - name: Build
        run: npm run build
        
      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
```

#### 选项 D: 使用自己的服务器

如果您有自己的服务器（阿里云、腾讯云等）：

**使用 Nginx：**

1. 将 `dist` 文件夹上传到服务器
2. 配置 Nginx：

```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    root /path/to/dist;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # 启用 gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
}
```

3. 重启 Nginx：
```bash
sudo nginx -t
sudo systemctl restart nginx
```

**使用 Apache：**

1. 将 `dist` 文件夹上传到服务器
2. 在 `dist` 文件夹中创建 `.htaccess`：

```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
```

## 部署前检查清单

### 必须完成的事项：

- [ ] 确保 `public/vehicle-detail-template.jpg` 图片已添加
- [ ] 运行 `npm run build` 确保构建成功
- [ ] 检查 `dist` 文件夹是否生成
- [ ] 测试本地预览：`npm run preview`

### 可选优化：

- [ ] 压缩图片资源
- [ ] 配置自定义域名
- [ ] 设置 HTTPS 证书
- [ ] 配置 CDN 加速

## 本地预览生产版本

构建完成后，可以本地预览生产版本：

```bash
npm run preview
```

默认访问地址：`http://localhost:4173`

## 环境变量配置（如需要）

如果需要配置不同环境的 API 地址等，创建 `.env.production` 文件：

```env
VITE_API_URL=https://api.your-domain.com
VITE_APP_TITLE=二手车拍卖平台
```

在代码中使用：
```javascript
const apiUrl = import.meta.env.VITE_API_URL;
```

## 常见问题

### Q1: 部署后页面刷新 404

**原因：** SPA 应用需要配置路由回退到 index.html

**解决方案：**
- Vercel/Netlify：自动处理
- Nginx：使用上面的配置
- Apache：使用 .htaccess 配置

### Q2: 图片加载失败

**原因：** 图片路径不正确或文件未上传

**解决方案：**
- 确保图片在 `public` 文件夹中
- 使用绝对路径：`/vehicle-detail-template.jpg`
- 检查文件名大小写

### Q3: 构建失败

**常见原因：**
- Node.js 版本过低（需要 14+）
- 依赖包未安装：运行 `npm install`
- 代码语法错误：检查控制台错误信息

### Q4: 部署后样式丢失

**原因：** 基础路径配置问题

**解决方案：**
在 `vite.config.js` 中配置：
```javascript
export default {
  base: '/', // 或者 '/your-repo-name/' for GitHub Pages
}
```

## 性能优化建议

### 1. 启用 Gzip 压缩

大多数托管平台默认启用，自己服务器需要配置。

### 2. 图片优化

```bash
# 安装图片压缩工具
npm install -D vite-plugin-imagemin

# 在 vite.config.js 中配置
import viteImagemin from 'vite-plugin-imagemin'

export default {
  plugins: [
    viteImagemin({
      gifsicle: { optimizationLevel: 7 },
      optipng: { optimizationLevel: 7 },
      mozjpeg: { quality: 80 },
    })
  ]
}
```

### 3. 代码分割

Vite 默认已启用，无需额外配置。

### 4. CDN 加速

将静态资源上传到 CDN（如阿里云 OSS、腾讯云 COS）。

## 监控和分析

### 添加访问统计

在 `index.html` 中添加：

```html
<!-- Google Analytics -->
<script async src="https://www.googletagmanager.com/gtag/js?id=YOUR-GA-ID"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'YOUR-GA-ID');
</script>

<!-- 或使用百度统计 -->
<script>
var _hmt = _hmt || [];
(function() {
  var hm = document.createElement("script");
  hm.src = "https://hm.baidu.com/hm.js?YOUR-BAIDU-ID";
  var s = document.getElementsByTagName("script")[0]; 
  s.parentNode.insertBefore(hm, s);
})();
</script>
```

## 推荐部署方案

**对于演示/测试：**
→ 使用 Vercel 或 Netlify（免费、快速、简单）

**对于生产环境：**
→ 使用自己的服务器 + Nginx + HTTPS

**对于开源项目：**
→ 使用 GitHub Pages（免费、稳定）

## 快速开始（推荐 Vercel）

1. 构建项目：
```bash
npm run build
```

2. 访问 [vercel.com](https://vercel.com)

3. 点击 "Add New Project"

4. 导入项目或拖拽 `dist` 文件夹

5. 等待部署完成（约 1-2 分钟）

6. 获得外网访问链接！

## 技术支持

如有部署问题，请参考：
- Vercel 文档：https://vercel.com/docs
- Netlify 文档：https://docs.netlify.com
- Vite 部署指南：https://vitejs.dev/guide/static-deploy.html

---

**部署完成后，请将外网访问链接记录在此：**

```
外网访问地址：https://___________________
部署时间：____________________
部署平台：____________________
```
